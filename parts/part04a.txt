0,0],scoreSubmitted:false,scoreStatus:'idle',todayBest:[]};side='left';rolling=false;loadDepth();setDieTransform(0,0);setFace(1);say('Tap the die.')};
const metaBaseSfx=sfx;
sfx=function(kind){if(kind==='zap'){tone(900,.06,'square',.03,-500);setTimeout(()=>tone(420,.09,'sawtooth',.022,-180),35);return}metaBaseSfx(kind)};
doMove=function(bonus=0){let moved=0,lastWhy='Nowhere useful to go.';const startDepth=S.depth;for(let i=0;i<1+bonus;i++){const m=chooseMove();if(!m)break;S.p={...m.step};lastWhy=m.why;moved++;sfx('move');collect();if(S.mode!=='play'||S.depth!==startDepth)break}S.msg=moved?lastWhy+(moved>1?' '+moved+' steps.':''):'Nowhere useful to go.'};
doSearch=function(bonus=0){let traps=0;const loot=[];const radius=3+bonus+(S.luck>=2?1:0);for(const t of S.traps)if(!t.shown&&dist(S.p,t)<=radius){t.shown=true;traps++}for(const l of S.loot)if(!l.shown&&dist(S.p,l)<=radius){l.shown=true;loot.push(l.name)}if(traps||loot.length){const bits=[];if(traps)bits.push(traps+' trap'+(traps===1?'':'s'));if(loot.length)bits.push(loot.join(', '));S.msg='Revealed '+bits.join(' + ')+'.'}else S.msg='Nothing useful nearby.';sfx('search')};
function metaGainXp(n=1){S.xp+=n;let levels=0;while(S.xp>=S.xpNeed){S.xp-=S.xpNeed;S.level++;S.xpNeed=S.level+1;S.pendingLevelUps++;levels++}return levels}
function upgradeFace(i){let target=i;if(faceUpgrade(target)>=3){const verb=ACTION[i];const alt=ACTION.map((v,j)=>({v,j,n:faceUpgrade(j)})).filter(x=>x.v===verb&&x.n<3).sort((a,b)=>a.n-b.n||a.j-b.j)[0];if(alt)target=alt.j;else return faceLabel(i)+' MAX'}S.faceUpgrades[target]=Math.min(3,faceUpgrade(target)+1);return faceLabel(target)}
doAttack=function(bonus=0){const near=S.enemies.filter(e=>dist(S.p,e)===1).sort((a,b)=>a.hp-b.hp);if(!near.length){S.msg='No Enemy adjacent.';sfx('attack');return}const e=near[0];let d=S.atk+bonus;if(S.luck>0&&Math.random()<.08*S.luck)d++;e.hp-=d;S.msg=e.name+' takes '+d+' damage.';sfx('attack');if(e.hp<=0){S.enemies=S.enemies.filter(v=>v!==e);const levels=metaGainXp(1);S.rewardQueued=true;if(levels){S.mode='levelup';S.msg=e.name+' defeated. LEVEL '+S.level+' — next roll upgrades that face.'}else{S.mode='reward';S.msg=e.name+' defeated — next roll is reward.'}sfx('reward')}};
doRest=function(bonus=0){const old=S.hp;S.hp=Math.min(S.max,S.hp+3+bonus+(S.luck>=3?1:0));S.msg='Recovered '+(S.hp-old)+' HP.';sfx('rest')};
function wizardHasLOS(e,p){if(!(e.x===p.x||e.y===p.y)||dist(e,p)>3)return false;const dx=Math.sign(p.x-e.x),dy=Math.sign(p.y-e.y);let x=e.x+dx,y=e.y+dy;while(x!==p.x||y!==p.y){if(wall(x,y))return false;x+=dx;y+=dy}return true}
monsterTurn=function(){if(S.mode!=='play')return;const dirs=[[0,-1],[1,0],[0,1],[-1,0]];for(const e of [...S.enemies]){if(e.t==='j'&&dist(e,S.p)<=3){S.charmed=2;S.msg+=' Saucy Enchantress charms the hero.'}if(e.t==='w'&&wizardHasLOS(e,S.p)){S.hp-=1;S.zapFx={wx:e.x,wy:e.y,px:S.p.x,py:S.p.y,until:performance.now()+320};S.msg+=' Wizard zaps -1 HP.';sfx('zap');continue}if(e.t==='t'&&e.hp<e.max&&S.turn%2===0)e.hp++;if(e.t==='n'&&S.turn%3===0&&S.enemies.length<5){const spot=dirs.map(([dx,dy])=>({x:e.x+dx,y:e.y+dy})).find(p=>!wall(p.x,p.y)&&!enemyAt(p.x,p.y)&&!(p.x===S.p.x&&p.y===S.p.y));if(spot){S.enemies.push({x:spot.x,y:spot.y,hp:3,max:3,t:'s',name:'REHIRED SKELETON'});S.msg+=' Necromancer rehires staff.';continue}}if(dist(e,S.p)===1){let d=Math.max(1,2-S.arm);if(S.luck>0&&Math.random()<.05*S.luck)d=0;S.hp-=d;S.msg+=' '+e.name+(d?' hits '+d+'.':' misses thanks to Luck.');if(d)sfx('hurt');continue}const opts=dirs.map(([dx,dy])=>({x:e.x+dx,y:e.y+dy})).filter(p=>!wall(p.x,p.y)&&!enemyAt(p.x,p.y)&&!(p.x===S.p.x&&p.y===S.p.y));opts.sort((a,b)=>dist(a,S.p)-dist(b,S.p));if(opts[0]&&dist(opts[0],S.p)<dist(e,S.p)){e.x=opts[0].x;e.y=opts[0].y}}if(S.charmed>0)S.charmed--;if(S.hp<=0){S.hp=0;S.mode='dead';S.msg='YOU DIED.';onGameOver()}};
