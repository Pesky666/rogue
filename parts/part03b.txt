function sprite(kind,x,y){const ht=HEX_TILE[kind];if(ht&&drawHex(ht.sheet,ht.i,x,y,ht.tint)){if(kind==='hero')R(x+1,y+1,4,2,C.cream);return}fallbackSprite(kind,x,y)}
function itemKind(i){return i.t==='gold'?'coin':i.t==='potion'?'food':i.t==='weapon'?'orb':i.t==='armour'?'ring':i.t==='curse'?'curse':i.t==='ring'?'ring':i.t==='orb'?'orb':'charm'}
function draw(){R(0,0,320,160,C.k);const stats=['HP '+S.hp+'/'+S.max,'ATK '+S.atk,'ARM '+S.arm,'GOLD '+S.gold,'LUCK '+(S.luck>=0?'+':'')+S.luck,'TURN '+S.turn];ctx.font='bold 8px monospace';ctx.textBaseline='top';ctx.textAlign='center';for(let i=0;i<6;i++){ctx.fillStyle=i===0?C.green:C.cream;ctx.fillText(stats[i],(i+.5)*(320/6),4)}ctx.textAlign='left';for(let y=0;y<H;y++)for(let x=0;x<W;x++){const X=x*T,Y=16+y*T;if(wall(x,y)){R(X,Y,T,T,C.cream);R(X+2,Y+2,5,3,C.dim);R(X+9,Y+8,5,3,C.dim)}else{R(X,Y,T,T,'#10110e');R(X+7,Y+7,1,1,C.dim)}}for(const f of S.food)sprite('food',f.x*T,16+f.y*T);for(const q of S.coin)sprite('coin',q.x*T,16+q.y*T);for(const i of S.items)sprite(itemKind(i),i.x*T,16+i.y*T);for(const l of S.loot.filter(v=>v.shown))sprite(itemKind(l),l.x*T,16+l.y*T);sprite('exit',S.exit.x*T,16+S.exit.y*T);for(const t of S.traps.filter(v=>v.shown))sprite('trap',t.x*T,16+t.y*T);for(const e of S.enemies)sprite(e.t,e.x*T,16+e.y*T);sprite('hero',S.p.x*T,16+S.p.y*T);R(PANEL,16,320-PANEL,128,C.k);ctx.strokeStyle=C.cream;ctx.strokeRect(PANEL+1,17,320-PANEL-2,126);ctx.font='bold 9px monospace';const verbs=ACTION.map((v,i)=>(i+1)+' '+(typeof metaVerb==='function'?metaVerb(v):v));verbs.forEach((v,i)=>{const yy=28+i*18;if(S.last===i+1){R(PANEL+6,yy-2,72,13,C.cream);ctx.fillStyle=C.k}else ctx.fillStyle=C.cream;ctx.fillText(v,PANEL+10,yy)})}
// v4 per-face run upgrades — reset on every run
let metaRollToken=0,metaWatchdog=null;
function faceUpgrade(i){return S&&S.faceUpgrades?S.faceUpgrades[i]||0:0}
function faceLabel(i){const n=faceUpgrade(i);return ACTION[i]+(n?' +'+n:'')}
const metaBaseLoad=loadDepth;
loadDepth=function(){S.zapFx=null;metaBaseLoad()};
resetRun=function(){const max=10;S={depth:0,p:{x:1,y:1},hp:max,max,atk:2,arm:0,gold:0,luck:0,turn:0,last:0,mode:'play',msg:'Tap the die.',food:[],coin:[],items:[],loot:[],exit:null,traps:[],enemies:[],charmed:0,level:1,xp:0,xpNeed:2,pendingLevelUps:0,rewardQueued:false,zapFx:null,faceUpgrades:[0,0,0,0,