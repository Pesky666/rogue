e(${rot}deg)`
function tone(f,d=.05,type='square',vol=.024,slide=0){try{if(!audio)audio=new(window.AudioContext||window.webkitAudioContext)();if(audio.state==='suspended')audio.resume();const o=audio.createOscillator(),g=audio.createGain();o.type=type;o.frequency.setValueAtTime(f,audio.currentTime);if(slide)o.frequency.linearRampToValueAtTime(Math.max(40,f+slide),audio.currentTime+d);g.gain.setValueAtTime(vol,audio.currentTime);g.gain.exponentialRampToValueAtTime(.001,audio.currentTime+d);o.connect(g);g.connect(audio.destination);o.start();o.stop(audio.currentTime+d)}catch(e){}}
function sfx(kind){if(kind==='roll'){tone(145,.035,'square',.023,90);setTimeout(()=>tone(210,.035,'square',.02,110),40);setTimeout(()=>tone(290,.04,'square',.018,130),80)}else if(kind==='land'){tone(390,.055,'triangle',.032,-30);setTimeout(()=>tone(620,.08,'square',.021,40),45)}else if(kind==='move')tone(130,.04,'square',.018,25);else if(kind==='attack')tone(90,.09,'sawtooth',.026,-25);else if(kind==='search')tone(760,.08,'triangle',.018,-240);else if(kind==='rest')tone(240,.11,'triangle',.018,75);else if(kind==='hurt')tone(70,.12,'square',.03,-25);else if(kind==='reward')tone(520,.1,'square',.02,80)}
function say(t){log.textContent=t}
function resetRun(){S={depth:0,p:{x:1,y:1},hp:10,max:10,atk:2,arm:0,gold:0,luck:0,turn:0,last:0,mode:'play',msg:'Tap the die.',food:[],coin:[],items:[],loot:[],exit:null,traps:[],enemies:[],charmed:0};side='left';rolling=false;loadDepth();setDieTransform(0,0);setFace(1);say('Tap the die.')}
function loadDepth(){const L=LEVELS[S.depth];S.p={x:1,y:1};S.food=L.food.map(v=>({...v}));S.coin=L.coin.map(v=>({...v}));S.items=L.items.map(v=>({...v}));S.loot=(L.loot||[]).map(v=>({...v}));S.exit={...L.exit};S.traps=L.traps.map(v=>({...v}));S.enemies=L.enemies.map(v=>({...v,max:v.hp}));S.mode='play';S.last=0;S.charmed=0;draw()}
const enemyAt=(x,y)=>S.enemies.find(e=>e.x===x&&e.y===y);
const trapAt=(x,y)=>S.traps.find(t=>t.shown&&t.x===x&&t.y===y);
const itemAt=(x,y)=>S.items.find(i=>i.x===x&&i.y===y);const lootAt=(x,y)=>S.loot.find(i=>i.shown&&i.x===x&&i.y===y);
function enemyDistance(p){return S.enemies.length?Math.min(...S.enemies.map(e=>dist(p,e))):99}
function mood(){const q=S.hp/S.max;return q<.25?'PANIC':q<=.5?'WARY':'BOLD'}
function bfs(target,adj=false,avoidTraps=true){const q=[{x:S.p.x,y:S.p.y,first:null}],seen=new Set([S.p.x+','+S.p.y]),dirs=[[0,-1],[1,0],[0,1],[-1,0]];while(q.length){const n=q.shift();if((!adj&&n.x===target.x&&n.y===target.y)||(adj&&dist(n,target)===1))return n.first;for(const[dx,dy]of dirs){const nx=n.x+dx,ny=n.y+dy,k=nx+','+ny;if(seen.has(k)||wall(nx,ny))continue;if(avoidTraps&&trapAt(nx,ny)&&!(nx===target.x&&ny===target.y))continue;const e=enemyAt(nx,ny);if(e&&!(adj&&e===target))continue;seen.add(k);q.push({x:nx,y:ny,first:n.first||{x:nx,y:ny}})}}return null}
function neighbours(){return [[0,-1],[1,0],[0,1],[-1,0]].map(([dx,dy])=>({x:S.p.x+dx,y:S.p.y+dy})).filter(p=>!wall(p.x,p.y)&&!enemyAt(p.x,p.y))}
function chooseMove(){const m=mood(),near=enemyDistance(S.p);if(m==='PANIC'&&near<=3){const ex=bfs(S.exit,false,true);if(ex&&enemyDistance(ex)>1)return{step:ex,why:'Low health — fleeing to Exit.'};const food=[];for(const f of S.food){const step=bfs(f,false,true);if(step)food.push({step,d:dist(S.p,f)})}food.sort((a,b)=>a.d-b.d);if(food[0])return{step:food[0].step,why:'Low health — seeking Food.'};const n=neighbours().filter(p=>!trapAt(p.x,p.y)).sort((a,b)=>enemyDistance(b)-enemyDistance(a));if(n[0])return{step:n[0],why:'Low health — avoiding Enemy.'}}
if(S.charmed>0){const lady=S.enemies.find(e=>e.t==='j');if(lady){const step=bfs(lady,true,true);if(step)return{step,why:'Charmed — pursuing Saucy Enchantress.'}}}
const goals=[];if(S.hp<S.max)S.food.forEach(v=>goals.push({rank:0,type:'Food',obj:v}));S.coin.forEach(v=>goals.push({rank:1,type:'Coin',obj:v}));S.loot.filter(v=>v.shown).forEach(v=>goals.push({rank:1.25,type:'Loot',obj:v}));goals.push({rank:2,type:'Exit',obj:S.exit});S.items.forEach(v=>goals.push({rank:2.5,type:'Magic',obj:v}));S.enemies.forEach(v=>goals.push({rank:3,type:'Enemy',obj:v,adj:true}));const ok=[];for(const g of goals){const step=bfs(g.obj,!!g.adj,true);if(step)ok.push({...g,step,d:dist(S.p,g.obj)})}ok.sort((a,b)=>a.rank-b.rank||a.d-b.d);if(ok[0])return{step:ok[0].step,why:ok[0].type==='Food'?'Injured — heading for Food.':ok[0].type==='Coin'?'Healthy — pursuing Coin.':ok[0].type==='Exit'?'Heading for Exit.':ok[0].type==='Magic'?'Magic nearby — investigating.':ok[0].type==='Loot'?'Loot revealed — collecting.':'Closing on Enemy.'};const n=neighbours().filter(p=>!trapAt(p.x,p.y));return n[0]?{step:n[0],why:'Choosing safe Floor.'}:null}
function collect(){let i=S.food.findIndex(v=>v.x===S.p.x&&v.y===S.p.y);if(i>=0){const old=S.hp;S.hp=Math.min(S.max,S.hp+4);S.food.splice(i,1);S.msg+=' Food +'+(S.hp-old)+' HP.'}i=S.coin.findIndex(v=>v.x===S.p.x&&v.y===S.p.y);if(i>=0){S.coin.splice(i,1);S.gold++;S.msg+=' Coin collected.'}const it=itemAt(S.p.x,S.p.y);if(it){if(it.t==='curse'){S.luck-=2;S.msg+=' Curse: Luck -2.'}else if(it.t==='ring'){S.luck+=2;S.msg+=' Ring: Luck +2.'}else if(it.t==='orb'){S.luck+=1;S.atk++;S.msg+=' Orb: Luck +1, ATK +1.'}else{S.luck++;S.msg+=' Charm: Luck +1.'}S.items=S.items.filter(v=>v!==it);sfx('reward')}const lo=lootAt(S.p.x,S.p.y);if(lo){if(lo.t==='gold'){S.gold+=2;S.msg+=' '+lo.name+': +2 Gold.'}else if(lo.t==='potion'){const old=S.hp;S.hp=Math.min(S.max,S.hp+4);S.msg+=' '+lo.name+': +'+(S.hp-old)+' HP.'}else if(lo.t==='weapon'){S.atk++;S.msg+=' '+lo.name+': ATK +1.'}else if(lo.t==='armour'){S.arm++;S.msg+=' '+lo.name+': ARM +1.'}else{S.luck++;S.msg+=' '+lo.name+': Luck +1.'}S.loot=S.loot.filter(v=>v!==lo);sfx('reward')}const tr=S.traps.find(t=>t.x===S.p.x&&t.y===S.p.y);if(tr){let d=3;if(S.luck>=2&&Math.random()<.35){d=0;S.msg+=' Luck avoids the Trap.'}S.hp-=d;S.traps=S.traps.filter(v=>v!==tr);if(d){S.msg+=' Trap! -'+d+' HP.';sfx('hurt')}}if(S.p.x===S.exit.x&&S.p.y===S.exit.y){if(S.depth<LEVELS.length-1){S.depth++;S.msg='Descending deeper.';loadDepth()}else{S.mode='win';S.msg='Escaped the dungeon.'}}}
function doMove(){const m=chooseMove();if(!m){S.msg='Nowhere useful to go.';return}S.p={...m.step};S.msg=m.why;sfx('move');collect()}
function doSearch(){let traps=0;const loot=[];const radius=3+(S.luck>=2?1:0);for(const t of S.traps)if(!t.shown&&dist(S.p,t)<=radius){t.shown=true;traps++}for(const l of S.loot)if(!l.shown&&dist(S.p,l)<=radius){l.shown=true;loot.push(l.name)}if(traps||loot.length){const bits=[];if(traps)bits.push(traps+' trap'+(traps===1?'':'s'));if(loot.length)bits.push(loot.join(', '));S.msg='Revealed '+bits.join(' + ')+'.'}else S.msg='Nothing useful nearby.';sfx('search')}
function doAttack(){const near=S.enemies.filter(e=>dist(S.p,e)===1).sort((a,b)=>a.hp-b.hp);if(!near.length){S.msg='No Enemy adjacent.';sfx('attack');return}const e=near[0];let d=S.atk;if(S.luck>0&&Math.random()<.08*S.luck)d++;e.hp-=d;S.msg=e.name+' takes '+d+' damage.';sfx('attack');if(e.hp<=0){S.enemies=S.enemies.filter(v=>v!