resolve=function(n,rollMode){if(rollMode==='levelup'){S.last=n;S.pendingLevelUps=Math.max(0,S.pendingLevelUps-1);const upgrade=upgradeFace(n-1);if(S.pendingLevelUps>0)S.mode='levelup';else if(S.rewardQueued)S.mode='reward';else S.mode='play';S.msg='FACE UPGRADE: '+upgrade+'.'+(S.mode==='reward'?' Next roll is reward.':'');draw();say(S.msg+'');return}S.last=n;if(rollMode==='reward'){const names=['','Rusty Sword +1 ATK','Questionable Mail +1 ARM','Mystery Pie +4 HP','Petty Cash +3 Gold','Lucky Charm +1 Luck','Full Restore'];if(n===1)S.atk++;if(n===2)S.arm++;if(n===3)S.hp=Math.min(S.max,S.hp+4);if(n===4)S.gold+=3;if(n===5)S.luck++;if(n===6)S.hp=S.max;S.rewardQueued=false;S.mode=S.pendingLevelUps>0?'levelup':'play';S.msg=names[n]+'.';draw();say(S.msg+'');return}S.turn++;const a=ACTION[n-1],bonus=faceUpgrade(n-1);if(a==='MOVE')doMove(bonus);else if(a==='SEARCH')doSearch(bonus);else if(a==='ATTACK')doAttack(bonus);else doRest(bonus);if(S.mode==='play')monsterTurn();draw();say(S.msg+'')};
finishRoll=function(n,rollMode,to,token){if(!rolling||token!==metaRollToken)return;if(metaWatchdog){clearTimeout(metaWatchdog);metaWatchdog=null}side=side==='left'?'right':'left';setDieTransform(to,0);setFace(n);try{sfx('land');resolve(n,rollMode)}catch(err){console.error('Roll resolution error',err);say('Roll error — tap.')}finally{rolling=false}};
rollDie=function(){if(rolling)return;if(S.mode==='dead'||S.mode==='win'){resetRun();return}rolling=true;const token=++metaRollToken;const rollMode=S.mode;const n=rollMode==='reward'?rewardRoll():1+Math.floor(Math.random()*6);const left=0,right=Math.max(0,space.clientWidth-die.offsetWidth),from=side==='left'?left:right,to=side==='left'?right:left,start=performance.now(),dur=720;sfx('roll');metaWatchdog=setTimeout(()=>finishRoll(n,rollMode,to,token),1500);function frame(now){if(!rolling||token!==metaRollToken)return;try{const t=Math.min(1,(now-start)/dur),ease=1-Math.pow(1-t,3),x=from+(to-from)*ease,rot=t*720*(side==='left'?1:-1);setDieTransform(x,rot);setFace(1+Math.floor(((now-start)/60)%6));if(t<1)requestAnimationFrame(frame);else finishRoll(n,rollMode,to,token)}catch(err){console.error('Roll animation error',err);finishRoll(n,rollMode,to,token)}}requestAnimationFrame(frame)};
const metaBaseDraw=draw;
draw=function(){metaBaseDraw();if(!S)return;ctx.font='bold 9px monospace';for(let i=0;i<6;i++){const yy=28+i*18;R(PANEL+5,yy-3,96,15,C.k);if(S.last===i+1){R(PANEL+6,yy-2,90,13,C.cream);ctx.fillStyle=C.k}else ctx.fillStyle=C.cream;ctx.textAlign='left';ctx.fillText((i+1)+' '+faceLabel(i),PANEL+10,yy)}ctx.font='bold 7px monospace';ctx.fillStyle=C.cream;ctx.textAlign='left';ctx.fillText('LV'+S.level+' XP',4,150);ctx.strokeStyle=C.cream;ctx.strokeRect(38,149,164,8);const xpW=Math.floor(162*Math.max(0,Math.min(1,S.xp/S.xpNeed)));if(xpW)R(39,150,xpW,6,C.green);if(S.zapFx){const now=performance.now();if(now<S.zapFx.until){ctx.strokeStyle=Math.floor(now/55)%2?C.red:C.cream;ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(S.zapFx.wx*T+8,16+S.zapFx.wy*T+8);ctx.lineTo(S.zapFx.px*T+8,16+S.zapFx.py*T+8);ctx.stroke();R(S.zapFx.px*T+5,16+S.zapFx.py*T+5,6,6,C.red);requestAnimationFrame(draw)}else S.zapFx=null}};

// v5 